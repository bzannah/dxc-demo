import { CommandModule } from 'yargs';

interface NewCmdOpts {
    name?: string;
    apiHost: string;
    outputPath?: string;
    disableTelemetry: boolean;
}
interface NewMeshCmdOpts {
    apiHost: string;
    outputPath?: string;
    disableTelemetry: boolean;
}
declare const uniformCLI: {
    commands: (CommandModule<unknown, NewCmdOpts> | CommandModule<unknown, NewMeshCmdOpts>)[];
};

export { uniformCLI };
